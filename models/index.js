const User = require('./user');
const Teacher = require('./teacher');
const Student = require('./student');
const Attendance = require('./attendance');

module.exports = { User, Teacher, Student, Attendance };